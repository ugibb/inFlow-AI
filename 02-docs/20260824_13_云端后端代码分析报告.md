# inFlow-ai FastAPI 后端分析报告

> 生成于 2026-08-24，基于对代码仓库的静态分析（Explore agent 只读扫描），配合前端调用关系对照。
> 分析对象：`03-src/server/inflow_server/`（后端）、`03-src/core/inflow_core/`（共享引擎）、`03-src/frontend/`（前端调用关系）。

---

## 0. 结论速览

后端「能跑」的功能面：采集 / 文章 / 知识库 / AI 问答 / 深度研究 / 微信绑定 / Obsidian 同步 / 系统管理。

**两个前端已接线但后端未挂载的功能 → 线上 404：**

| 功能 | 后端路由 | 前端调用方 |
|---|---|---|
| 学习路径 `/api/paths` | `routers/learning.py`（未在 main.py 挂载） | `src/app/paths/*`、api.ts 的 getPaths 等 |
| 周期回顾 `/api/review` | `extensions/review/router.py`（未挂载）；`review_cron_loop()` 也无任何启动点 | `ReviewSettings.tsx`（渲染于 `app/my/page.tsx`） |

**无调用方的死代码 / 死依赖：**

- `extensions/review/service.py::review_cron_loop` — 定时推送回顾的 cron，写了但从未启动
- `extensions/review/service.py` 的 `generate_review_text`、`compute_next_send_at`、`render_for_wechat`、`_split_for_wechat` — 只被死 router + 死 cron 引用
- `extensions/obsidian/writer.py` 全部（`ObsidianSyncer` 等）— 本地同步客户端库放错仓库，后端零引用
- core `requirements.txt` 的 `readability-lxml` — 全代码零 import

---

## 1. main.py — 应用组装与生命周期

**挂载的 router（共 10 组）**：

| router | 最终路径 |
|---|---|
| articles.router | `/api/articles` |
| knowledge.router | `/api/knowledge` |
| system.router | `/api/system` |
| assistant.router | `/api/assistant/*` |
| auth.router | `/api/auth` |
| users.router | `/api/users` |
| extensions/wechat/router | `/api/wechat` |
| extensions/research/router | `/api/research` |
| extensions/obsidian/router | `/api/sync` |
| inflow_core/ingest/router | `/api/ingest` |

**main.py 直接定义：**
- `GET /api/images/proxy` — 反代微信/小红书/抖音图片 CDN（防 SSRF，白名单域名），正文 markdown 内嵌图片用它加载
- `GET /api/health` — 健康检查

**中间件**：CORS、`OptionsHandler`（预检 405 修复）、`ApiNoCacheMiddleware`。

**生命周期 `lifespan`**：启动只做 `init_db()`（建表 + 跑迁移）+ 打一条 `external_processing` 警告日志。**不启动任何后台任务**（无 wechat-bot、无 review cron、无 APScheduler/Celery）。注释明确：云端只登记，重计算交给本地 worker。

---

## 2. routers/ 全部 endpoint 清单

### 2.1 `routers/articles.py`（`/api/articles`）— 已挂载

依赖：`ingest_url/ingest_upload/ingest_text`、`parser_service`、`llm_service`、`graph_service`、`spark_service.generate_article`、IngestJob/Article/Tag/Folder 模型。

| 方法 | 路径 | 功能 |
|---|---|---|
| POST | `/api/articles` | 按 URL 添加文章，返回 job_id（异步管道） |
| POST | `/api/articles/batch` | 批量按 URL 添加 |
| PATCH | `/api/articles/batch-move` | 批量移动到文件夹 |
| POST | `/api/articles/manual` | 粘贴内容建文章（BeautifulSoup 提取） |
| POST | `/api/articles/notes` | 创建 Markdown 笔记 |
| POST | `/api/articles/spark` | 一句话→AI 生成整篇文章入库 |
| POST | `/api/articles/upload` | 上传文件（PDF/docx/图片等，MarkItDown 转换） |
| GET | `/api/articles` | 列表 + 语义/关键词搜索 + 筛选分页 |
| GET | `/api/articles/{id}/chapters` | 返回章节（读 chapters.json） |
| GET | `/api/articles/{id}/transcript` | 返回 ASR 转录分段 |
| GET | `/api/articles/{id}/deep-read` | 返回 AI 深度阅读卡片 HTML |
| POST | `/api/articles/{id}/deep-read/screenshot` | 返回卡片 PNG（worker 回传） |
| POST | `/api/articles/{id}/regenerate-ai` | 音频文章重跑 AI（章节+摘要） |
| GET | `/api/articles/{id}/regen-status` | 重生成进度 |
| GET | `/api/articles/{id}` | 文章详情 |
| PATCH | `/api/articles/{id}` | 更新元数据 |
| DELETE | `/api/articles/{id}` | 删除（同时取消 ingest job） |
| PATCH | `/api/articles/{id}/tags` | 整体替换标签 |
| POST | `/api/articles/{id}/reprocess` | 重跑 AI 处理 |
| GET | `/api/articles/{id}/related` | 相关文章（按关系类型分组） |
| POST | `/api/articles/backfill-embeddings` | 补生成缺失 embedding |

### 2.2 `routers/knowledge.py`（`/api/knowledge`）— 已挂载

依赖：Tag/Folder/Article/KnowledgeEdge/LearningPath 模型、`graph_service`、`llm_service`。

| 方法 | 路径 | 功能 |
|---|---|---|
| GET/POST | `/api/knowledge/tags` | 列出/创建标签 |
| PATCH/DELETE | `/api/knowledge/tags/{id}` | 更新/删除标签 |
| GET | `/api/knowledge/tags/stats` | 标签+文章数 |
| POST | `/api/knowledge/tags/merge` | 合并标签 |
| POST | `/api/knowledge/tags/batch-delete` | 批量删除标签 |
| GET/POST | `/api/knowledge/folders` | 列出/创建文件夹 |
| PATCH/DELETE | `/api/knowledge/folders/{id}` | 更新/删除文件夹 |
| GET | `/api/knowledge/graph` | 知识图谱全量数据 |
| POST | `/api/knowledge/graph/regenerate` | 后台重建图谱（LLM 分批） |
| GET | `/api/knowledge/graph/article/{id}` | 单文章为中心的子图 |
| GET | `/api/knowledge/stats` | 库统计 |
| GET/POST/DELETE | `/api/knowledge/mindmap/{id}` | 读取/生成/清除思维导图缓存 |

### 2.3 `routers/auth.py`（`/api/auth`）— 已挂载

- POST `/api/auth/login` — 登录发 JWT
- GET `/api/auth/me` — 当前用户信息
- POST `/api/auth/change-password` — 改密码

### 2.4 `routers/users.py`（`/api/users`）— 已挂载（超管）

- GET/POST/PATCH/DELETE — 用户 CRUD

### 2.5 `routers/system.py`（`/api/system`）— 已挂载（超管）

- GET `/api/system/stats` — 缓存/版本统计
- DELETE `/api/system/cache` — 清前端 .next 缓存并重启容器
- POST `/api/system/rebuild` — 重建前端镜像
- GET `/api/system/config` — 读配置（掩码）
- POST `/api/system/config/{group}/test` — 连接测试
- PUT `/api/system/config/{group}` — 保存配置（先测后存）

### 2.6 `routers/assistant.py`（`/api/assistant`）— 已挂载

- POST `/api/assistant/ask` — RAG 问答：embedding 语义检索 → LLM 回答带引用；带 `article_id` 时切「本文问答」

### 2.7 `routers/learning.py`（`/api/paths`）— **死 router，未挂载**

- GET `/api/paths`、POST `/api/paths/generate`、GET/PATCH/DELETE `/api/paths/{id}` — 学习路径 CRUD + AI 生成
- **未在 main.py import/include**。前端 api.ts + `/paths` 页面在调用 → **前端调了，后端 404**

### 2.8 `routers/__init__.py` — 空文件

---

## 3. extensions/ 扩展分析

### 3.1 obsidian（已挂载，`/api/sync`）

- **功能**：Obsidian 本地同步，给外部插件 inFlow-sync-obsidian 提供 API
- POST `/api/sync/issue-token`（1 年 JWT）、POST `/api/sync/revoke-all-tokens`、GET `/api/sync/articles`（keyset 分页）、POST `/api/sync/articles`（Obsidian→inFlow 推送）、GET `/api/sync/stats`、GET `/api/sync/plugin-download`
- **`writer.py`（ObsidianSyncer）**：本地客户端写入器，**全仓库无任何 import** — 给外部本地仓库用的客户端库，躺在后端里属死代码

### 3.2 research（已挂载，`/api/research`）

- POST `/api/research/ask`（4 阶段：plan→retrieve→synthesize→critique，SSE）、POST `/api/research/agent`（ReAct 工具型 agent，SSE）
- `agent.py`/`tool_agent.py` 均被 router import → 存活；前端 AIAssistant 与微信 bot 都在调用

### 3.3 review（**未挂载**）

- **功能**：周期知识回顾（LLM 汇总推送到微信）
- `router.py`（`/api/review`）：GET/PUT `/api/review/schedule`、POST `/api/review/preview` — **全仓库无 import** → 死 router；前端 ReviewSettings 在调 → **404**
- `service.py`：
  - `send_wechat()` — 存活（被 `articles.py::_maybe_push_proactive_relation` 复用，spark 建文后的相似文章推送）
  - `generate_review_text()` / `compute_next_send_at()` — 只被死 router 引用 → 实际死代码
  - `review_cron_loop()` — **定义了但从未被任何地方启动**（main.py lifespan 不启、start-server.sh 不启）

### 3.4 wechat（router 已挂载；bot 独立进程）

- **`router.py`（`/api/wechat`）**：POST `/api/wechat/bind/start`（向 ilinkai 要二维码→渲染 PNG）、GET `/api/wechat/bind/status`、GET `/api/wechat/status`、GET `/api/wechat/account`、DELETE `/api/wechat/account`
- **`bot.py`**：**不挂在 FastAPI 里**。由 start-server.sh 独立进程启动（`python -m inflow_server.extensions.wechat.bot`，需 `inFlow_BASE`/`inFlow_TOKEN`）。`BotSupervisor` 读活跃账号 → 每账号一个 `AccountWorker` 长轮询 `/ilink/bot/getupdates`，处理 URL 收藏与 `/r /a /c /help` 命令，收件后回调 `wechat_callback_queue` 表推送卡片。作为 HTTP 客户端调 `/api/ingest/url`、`/api/research/*`、`/api/articles/spark`、`/api/assistant/ask`
- **`ilink_media.py`**：上传图片到 iLink CDN，被 bot.py 使用 → 存活

---

## 4. 依赖检查

### server `requirements.txt`（已「瘦身」）

- 无 playwright/markitdown/fastembed/whisper 等重库
- 无明显「只在死代码里 import」的重量级库

### core `requirements.txt` / `pyproject.toml`

- `pyproject.toml` 用 `dynamic = ["dependencies"]` 把 core requirements 作为包运行时依赖 → 任何人 `pip install -e ../core` 会连坐安装全部重型依赖
- 重型依赖（markitdown[all]、playwright、fastembed、faster-whisper、yt-dlp、bilibili-api-python、groq、aliyun oss2、curl-cffi、trafilatura）均在 core 代码中被 import（供本地 worker 用），云端 server 靠 `--no-deps` 规避
- **明确无用**：`readability-lxml`（core requirements.txt 第 20 行，全代码零 import）

---

## 5. 关键 grep 结论

| 检查项 | 结果 |
|---|---|
| research/agent.py、tool_agent.py | 被 router import → 存活 |
| extensions/review 是否挂载 | **否**；`review_cron_loop` 无启动方；仅 `send_wechat` 被 articles.py 复用 |
| wechat/bot.py 如何启动 | 独立进程 `python -m inflow_server.extensions.wechat.bot`，PID 文件 `.server/wechat-bot.pid` |
| routers/learning.py 是否挂载 | **否** |
| obsidian/writer.py | 全仓库无引用 → 死代码 |

---

## 6. 从未被挂载 / 从未被调用的后端代码

**从未被挂载的 router（前端却在调用 → 线上 404）：**
1. `routers/learning.py` → `/api/paths` 系列（前端 api.getPaths 等 + `/paths` 页面在用）
2. `extensions/review/router.py` → `/api/review/schedule`、`/api/review/preview`（前端 ReviewSettings 在用）

**定义了但无调用方：**
3. `extensions/review/service.py::review_cron_loop` — 回顾定时推送 cron，从未启动
4. `review/service.py` 的 `generate_review_text`、`compute_next_send_at`、`render_for_wechat`、`_split_for_wechat` — 只被死 router/cron 引用
5. `extensions/obsidian/writer.py` 全部 — 本地同步客户端库，后端零引用
6. core `readability-lxml` 依赖 — 零 import

**使用面很窄（近乎死，但有一个活入口）：**
- `review/service.py::send_wechat` — 唯一活入口是 spark 建文后的相似文章推送
- `articles.py::process_article_background` — 只被 `/api/articles/spark` 的 background task 引用（旧版云端 AI 后处理，已让位给 ingest job + worker）

---

*本文档由代码静态分析自动生成，供云端重构讨论参考。*
