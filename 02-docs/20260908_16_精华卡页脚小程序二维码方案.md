# 精华卡页脚小程序二维码方案

> 状态：已实施（2026-09-08）
> 日期：2026-09-08
> 需求：AI精读 tab 的 HTML 页面及 PNG 图片的「区块9：页脚」增加本篇文章的微信小程序码，用户扫码直达小程序内本文

## 决策记录（2026-09-08 确认）

1. 小程序**已发布线上版** → `env_version=release`（默认）。不做 release 失败自动降 trial——静默生成体验版码多数用户打不开，比无码更糟；失败仅告警无码渲染。
2. 码下文案：**「微信扫码 · 小程序读全文」**。
3. 存量文章**暂不补码**，先新增文章验证后再补（补码=对旧 job 重跑 compose）。
4. 权限边界：**持码游客即可读**——后端新增游客只读通道（UUID 即阅读凭证），扫码者无需登录、无需是文章所有者。

---

## 一、现状梳理（关键事实）

1. **前端只是展示壳**：`03-src/frontend/components/deep-read-panel.tsx` 渲染的是 `articles.deep_read_html`（worker 直写库）；点「截图」下载的 PNG 也是 worker 预渲染回传的——`backend/routers/articles.py` 的 screenshot 端点只按 job 回读 `03_display/{job_id}.png`，前端传的 html 已被忽略（兼容旧契约）。
   → **本仓库（inFlow-ai）前端/后端都不用改**，真正要动的是 worker 仓库（本地 `inflow-local`，git remote 为 `ugibb/inflow-worker`）。
2. **HTML 由 LLM 按 Prompt 生成**：区块9 页脚规格在 worker 的 `05-src/skills/_shared/render/prompts/s4/20250413-播客访谈记录生成网页Prompt.md`（区块 9：页脚，必须输出）。二维码不能让 LLM 自己画（必然画成假码），必须**程序化注入**。
3. **一注入点覆盖三产物**：`render_card_png_for_job` 中 LLM 出 HTML 后、写盘截图前注入，同一份 HTML 覆盖：HTML 落盘（`{job_id}.html`）→ PNG 截图（Playwright）→ `deep_read_html` 写库（executor `_persist_final_assets_to_article` 读的就是这个文件）。
4. **微信凭据已就绪**：云端后端已有 `WECHAT_APPID / WECHAT_SECRET`（微信一键登录在用），appid `wxe091ce737f45cacc` 与 `miniprogram/project.config.json` 一致。worker 侧只需把同值加进 `.env.local-worker`。

---

## 二、技术选型

### 码类型：微信官方小程序码（`wxa/getwxacodeunlimit`）

- 微信内**长按识别 / 相机扫码**都能直达小程序指定页，是唯一不依赖「已备案域名 + 扫普通链接二维码打开小程序」控制台配置的稳妥路径。
- 数量不限（区别于 `wxacode.get` 的 10 万个总量上限），适合按文章一码。

### scene 传参：UUID 去横线

- 文章 id 为 36 位 UUID（含 4 个横线），超过 scene 的 32 字符上限；**去掉横线恰好 32 位 hex**，正好卡在限制内。
- 无需给 articles 加短 id 列、无需 base62 编解码。
- 小程序 read 页 `onLoad` 补 `options.scene` 解析（decodeURIComponent → 32 位 hex → 还原带横线 UUID），登录闭环复用现有 redirect 逻辑（`pages/read/read.ts` 已支持无 token 走登录重定向）。
- scene 字符集仅支持数字/大小写英文及 `!#$&'()*+,/:;=?@-._`——hex（0-9a-f）全部合法。

### 页脚布局（区块9 品牌区左侧，内联样式自适应明暗主题）

```
┌────────────────────────────────────────────────┐
│ 来源：小宇宙            ┌────┐  inFlow AI       │
│ 主播 · 嘉宾 · EP 69     │ ▓▓ │  内容精华提炼     │
│                        └────┘                  │
│                  微信扫码 · 小程序读全文          │
└────────────────────────────────────────────────┘
```

- 二维码块自带白色底 + 圆角 + padding（小程序码本身白底，深色主题下也清晰）。
- 品牌区文字（inFlow AI / 内容精华提炼）保持现状，码放其左侧。

---

## 三、改动清单（已实施）

### worker 仓库（inflow-local / ugibb/inflow-worker）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `05-src/utils/settings.py` | WorkerSettings 增加 `wechat_appid / wechat_secret / wxacode_env_version`（默认 release） |
| 2 | 新增 `05-src/skills/_shared/render/wxacode.py` | stable_token 内存缓存（过期前 5 分钟刷新，40001/42001 强刷重试一次）；调 `getwxacodeunlimit`（body：scene、page=`pages/read/read`、check_path=false、width=430、env_version 取配置）；按 article_id 落盘缓存 PNG（`04-output/_wxacode/{hex}.png`，一篇一码永久复用） |
| 3 | `05-src/skills/_shared/render/card_renderer.py` | `render_card_png_for_job` 增加 `article_id` 参数；`_llm_generate_card_html` 返回后、`html_path.write_text` 前注入：占位符 `<!--MP-QRCODE-->` 原位替换，缺失兜底插 `</footer>` 前（再兜底 `</body>` 前，右对齐包裹）。注入块自包含内联样式（img data-URI + caption），不依赖主题 CSS |
| 4 | `05-src/skills/_shared/render/prompts/s4/20250413-播客访谈记录生成网页Prompt.md` | 区块9 品牌区改为横向 flex：左侧原样保留 `<!--MP-QRCODE-->` 占位注释，右侧品牌文字；**明确禁止 LLM 自绘二维码/占位图形** |
| 5 | `05-src/framework/runtime/executor.py` | `_run_compose` 增加 `article_id` 形参，`_compose_and_continue` 调用处透传 |

### 云端后端（本仓库 inFlow-ai）

| # | 文件 | 改动 |
|---|---|---|
| 6 | `03-src/backend/routers/articles.py` | 新增游客只读通道三条路由（无鉴权，仅 GET）：`GET /{article_id}/guest`（detail，剥离 raw_content/收藏/文件夹/标签）、`/{article_id}/guest/chapters`、`/{article_id}/guest/transcript`（均镜像登录版逻辑，含 total_duration 回填与 content_blocks 快照） |

### 小程序（本仓库 inFlow-ai）

| # | 文件 | 改动 |
|---|---|---|
| 7 | `03-src/miniprogram/utils/api.ts` | 新增 `getArticleGuest / getChaptersGuest / getTranscriptGuest`（skipAuth 不注 token） |
| 8 | `03-src/miniprogram/pages/read/read.ts` | `resolveEntryId`：`options.scene`（32 位 hex）→ 还原 UUID；scene 进入支持游客只读（无 token 或 404 非本账号文章 → `enterGuest()` 走 /guest 通道）；章节/转录懒加载与重试按 guest 标记切通道；普通/分享卡片进入行为不变 |

### 配置

- `.env.local-worker`（及 example 模板）追加 `WECHAT_APPID`（已填 `wxe091ce737f45cacc`）/ `WECHAT_SECRET`（**待填，与云端 .env 同值**）/ 可选 `WXACODE_ENV_VERSION`。

### 本仓库 Web 前端

- **零改动**。AI精读 tab 的 HTML 展示与 PNG 下载均消费 worker 产物，注入后自动带码。

---

## 四、降级策略

| 场景 | 行为 |
|---|---|
| worker 未配置微信凭据（当前 `WECHAT_SECRET` 待填即此态） | 照常出卡不加码，info 日志提示 |
| 微信 API 失败（网络/errcode，含 40001/42001 强刷 token 重试一次后仍失败） | 无码渲染 + warning 日志，**绝不阻断管道** |
| article_id 缺失（极老 job 无关联） | 无码渲染 |
| LLM 忘写占位符 | 兜底注入 `</footer>` 前 / `</body>` 前 |
| 小程序端（旧线上版无 scene 解析） | 落地「缺少文章参数」错误页——**小程序需发新版本后二维码才可用** |

---

## 六、验证方式与上线步骤

1. `.env.local-worker` 填入 `WECHAT_SECRET`（与云端 .env 同值）。
2. worker 重启后跑一单新 ingest：确认 `{job_id}.html` 页脚含 data-URI 二维码、`{job_id}.png` 截图中二维码清晰、web 端 AI精读 tab 同步带码。
3. 微信扫码：进小程序 read 页、文章正确、未登录游客直接可读（章节/转录/音频照常）。
4. 云端后端部署（git pull + restart）后 guest 三接口生效。
5. **小程序发版**（上传 → 提审 → 发布）：线上版无 scene 解析，未发版前扫码会报「缺少文章参数」。开发者工具可用「编译模式 → 添加编译参数 scene=<32位hex>」本地联调。
6. 断网/错 secret 模拟降级：卡片正常产出、无码、仅 warning。
