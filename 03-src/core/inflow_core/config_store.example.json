{
  "_comment": "Runtime UI overrides only (plugins, etc.). LLM / Embedding API keys belong in .env — see .env.example.",
  "plugins": {
    "enable_yt_dlp": "true",
    "proxy": "",
    "enable_asr": "true",
    "asr_max_duration": "1800"
  }
}
