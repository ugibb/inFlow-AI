<view class="transcript">
  <view class="seg" wx:for="{{view}}" wx:key="idx" data-idx="{{index}}" bindtap="onTap">
    <text class="seg-time">{{item.time}}</text>
    <text class="seg-text" user-select>{{item.text}}</text>
  </view>
</view>
