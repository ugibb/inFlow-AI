# inflow backend 云端服务依赖（瘦身版，单包合并后一次装全）
#
# 云端定位 = 功能交互及展示：重计算（采集/转录/parse/compose/截图）全部由
# 本地 worker 承接，云端 venv 不装 playwright/markitdown/fastembed/whisper 等
# worker 重引擎（对应 lazy import，未安装时功能保持现状）。
# 安装：pip install -r backend/requirements.txt（不再需要 --no-deps 规避）
uvicorn[standard]==0.30.6
python-multipart==0.0.12  # FastAPI 文件上传/表单
fastapi==0.115.0
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9
pgvector==0.3.3
httpx==0.27.2
h2==4.1.0
pydantic==2.9.2
pydantic-settings==2.5.2
beautifulsoup4==4.12.3
lxml==5.3.0
# trafilatura/justext 在 lxml>=5.2 需要(lxml.html.clean 已拆为独立包)
lxml_html_clean>=0.4.0
markdownify==0.13.1
json-repair>=0.30.0
jinja2==3.1.4
alembic==1.13.3
qrcode[pil]>=7.4.2  # 微信扫码绑定：把 ilinkai 返回的 URL 渲染成 PNG data URI
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 不兼容 bcrypt>=4.1（缺少 __about__）
bcrypt==4.0.1
pytest>=8.0.0
